import styled from 'styled-components';

const CBIcon = styled.svg`
  fill: none;
  stroke: ${({ theme }) => theme.colors.base};
  stroke-width: 4px;
  width: 16px;
  height: 16px;
`;

export default CBIcon;
